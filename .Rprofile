source("renv/activate.R")

# Increase time-limit for downloads
options(timeout = max(1000, getOption("timeout")))

options(width = 200)

# Resolve conflicts
source("R/resolve_conflicts.R")

# Return tibble from taxastand functions by default
options(ts_tbl_out = TRUE)